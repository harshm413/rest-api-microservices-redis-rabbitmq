apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: prod
  labels:
    app: auth-service
spec:
  selector:
    app: auth-service
  ports:
  - port: 4003
    targetPort: 4003
    protocol: TCP
  type: ClusterIP
