apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: prod
  labels:
    app: user-service
spec:
  selector:
    app: user-service
  ports:
  - port: 4001
    targetPort: 4001
    protocol: TCP
  type: ClusterIP
