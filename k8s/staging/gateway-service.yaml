apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: staging
  labels:
    app: gateway-service
spec:
  selector:
    app: gateway-service
  ports:
  - port: 4000
    targetPort: 4000
    protocol: TCP
  type: ClusterIP
